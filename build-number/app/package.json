{
  "name": "build-number-demo-app",
  "version": "1.0.0",
  "private": true,
  "description": "Simple app for CI build number versioning demo",
  "scripts": {
    "build": "mkdir -p dist && node src/index.js > dist/version.txt"
  }
}
